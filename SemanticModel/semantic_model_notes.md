# Semantic Model

The semantic model is created on top of Warehouse views.

## Tables Used

- vw_customer_engagement
- vw_campaign_performance
- vw_support_summary

Each view supports a separate reporting domain.